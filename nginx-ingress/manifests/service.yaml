apiVersion: v1
kind: Service
metadata:
  name: nginx-app
spec:
  type: NodePort
  selector:
    app: nginx-app
  ports:
  - name: port80-80
    port: 80
    targetPort: 80
    nodePort: 30080
    protocol: TCP
