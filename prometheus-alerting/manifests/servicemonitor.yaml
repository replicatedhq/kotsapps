apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: prometheus-oper-flaky-app
  namespace: monitoring
  labels:
    app: prometheus-alerting
spec:
  jobLabel: flaky-app
  selector:
    matchLabels:
      app: flaky-app
  namespaceSelector:
    any: true
  endpoints:
    - port: http
      interval: 5s
      scheme: http
      path: /metrics
